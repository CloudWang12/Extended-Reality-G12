using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IA_Bed : InteractiveActorBase
{
    // Start is called before the first frame update 
    
    void Start()
    {
        DebugActor();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
